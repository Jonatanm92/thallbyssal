# Thallbyssal Factory Queue

Safe 24h multi-agent queue for parallel Codex work. Do not implement these tasks from this queue file. Each task must run on its own branch and must obey `AGENTS.md`.

| task_id | agent_lane | priority | branch_name | status | allowed_paths | forbidden_paths | goal | stop_condition | validation_commands | requires_founder_approval |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TASK_001 | render-discovery | P0 | `codex/task-001-headless-render-discovery` | ready | `AMP_SIM_LAB/HEADLESS_RENDER_DISCOVERY.md`, docs-only inspection notes | `native/juce-audio-engine/Source/ThallLabDspEngine.*`, `native/juce-audio-engine/Source/PluginProcessor.*`, DSP/core sound files | Inspect whether a safe headless/offline render command can be created and output `AMP_SIM_LAB/HEADLESS_RENDER_DISCOVERY.md`. | Stop if implementation needs DSP/core changes, GUI automation, or fake render behavior. | `npm run lab:validate`; `npm test -- --run`; `npm run build` | Yes if implementation would touch DSP/core sound or native tone behavior |
| TASK_002 | render-safety | P0 | `codex/task-002-render-safety-validators` | ready | `AMP_SIM_LAB/test-harness/render-hook`, `AMP_SIM_LAB/reports` | DSP/core sound files, original DI files, GUI automation scripts | Strengthen render safety validators for unchanged DI, no GUI automation, no fake render, render-root-only outputs, and no DSP files touched. | Stop if validator requires changing render DSP/core behavior. | `npm run lab:render:safety`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No unless protected files must change |
| TASK_003 | baseline-reports | P1 | `codex/task-003-baseline-regression-reports` | ready | `AMP_SIM_LAB/test-harness/render-hook`, `AMP_SIM_LAB/reports` | DSP/core sound files, preset tone values, original DI files | Improve baseline create/compare reports for metrics only. | Stop if task starts judging tone quality or tries to auto-fix sound. | `npm run lab:baseline:create`; `npm run lab:baseline:compare`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No |
| TASK_004 | preset-validation | P1 | `codex/task-004-preset-validation-hardening` | ready | `AMP_SIM_LAB/test-harness`, `AMP_SIM_LAB/presets`, `AMP_SIM_LAB/reports`, `AMP_SIM_LAB/PRESET_SCHEMA.md` | DSP/core sound files, approved preset tone values unless only schema/examples are requested | Improve preset schema validation, duplicate detection, invalid category detection, suspicious claims, and HTML summary. | Stop if validation would require changing approved tone values. | `npm run lab:presets`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No unless changing tone values |
| TASK_005 | demo-audition | P2 | `codex/task-005-demo-audition-page` | ready | `AMP_SIM_LAB/demo-site`, `AMP_SIM_LAB/test-harness`, `AMP_SIM_LAB/reports` | DSP/core sound files, public launch code, checkout/auth/telemetry | Create internal demo/audition HTML page that lists rendered clips when present and works with placeholders/dry-run. | Stop if page becomes public marketing or needs external services. | `npm run lab:audition`; `npm run lab:report-index`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No |
| TASK_006 | beta-packet | P1 | `codex/task-006-beta-tester-packet-hardening` | ready | `AMP_SIM_LAB/beta-pack`, `AMP_SIM_LAB/BETA_TESTER_PACKET.md`, `AMP_SIM_LAB/test-harness`, `AMP_SIM_LAB/reports` | DSP/core sound files, licensing, DRM, auth, telemetry, checkout | Improve private beta instructions, bug template, tone feedback template, install/uninstall placeholders, known issues, and disclaimers. | Stop if task adds public beta, user tracking, licensing, DRM, or checkout. | `npm run lab:beta-readiness:strict`; `npm run lab:beta-pack`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No |
| TASK_007 | ci-plan | P2 | `codex/task-007-ci-build-matrix-plan` | ready | `AMP_SIM_LAB/release-checklists`, docs-only CI/build plan files | Paid services, public release pipeline, deployment config, secrets | Create Windows/macOS CI/build matrix plan for later. | Stop before adding real CI, paid services, secrets, or release automation. | `npm run lab:validate`; `npm test -- --run`; `npm run build` | No |
| TASK_008 | review-agent | P0 | `codex/task-008-review-agent-packet` | ready | `TASK_PACKETS/TASK_008_REVIEW_AGENT_PACKET.md`, docs-only review checklist | Any code files unless explicitly asked in a future review task | Create a review-only Codex agent checklist for P0/P1 issues. | Stop if task starts writing code or editing implementation files. | `npm run lab:validate`; `npm test -- --run`; `npm run build` | No |
| TASK_009 | report-index | P2 | `codex/task-009-report-index-polish` | ready | `AMP_SIM_LAB/test-harness/generate-report-index.mjs`, `AMP_SIM_LAB/reports` | DSP/core sound files, public launch code, external services | Improve internal report index UX with clear links and status badges. | Stop if task changes report generation into public demo/launch flow. | `npm run lab:report-index`; `npm run lab:test`; `npm test -- --run`; `npm run build` | No |
| TASK_010 | operating-plan | P1 | `codex/task-010-usage-saving-factory-plan` | ready | `TASK_PACKETS/TASK_010_USAGE_SAVING_FACTORY_PLAN.md`, docs-only operating plan files | DSP/core sound files, automation that changes code without review | Create usage-efficient Codex operating plan: small prompts, small branches, fewer repeated builds, clear stop conditions. | Stop if plan encourages bypassing validation or founder approval. | `npm run lab:validate`; `npm test -- --run`; `npm run build` | No |

## Factory Notes

- Agents must read `AGENTS.md` before starting a task.
- Agents must read `CODEX_PROJECT_THALLBYSSAL/AUTONOMY_RULES.md` before starting a task.
- Safe assigned tasks are pre-approved; agents should implement and validate without asking for founder approval when they stay inside allowed paths and avoid stop conditions.
- Agents must not assume founder approval carries across tasks.
- Agents must report whether DSP/core was touched.
- Agents must report whether original DI files were changed.
- Agents must keep generated outputs under approved `D:\CodexBuilds\thallbyssal-lab` lab folders.
