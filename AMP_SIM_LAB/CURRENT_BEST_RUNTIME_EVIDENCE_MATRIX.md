# Current Best Runtime Evidence Matrix

Generated: 2026-06-28

## Scope

This document records the runtime-evidence target used for Current Best source recovery.

It is not a tone approval, source-parity approval, release approval, or product integration approval.

## Boundaries

- Product DSP changed: no.
- Golden Reference A changed: no.
- Product default changed: no.
- NAM/IR/audio/model assets committed: no.
- Original DI files changed: no.
- Generated runtime evidence reports remain under `D:\CodexBuilds\thallbyssal-lab\reports`.

## Tooling

Runtime evidence is generated by:

```bash
npm run lab:current-best:evidence
```

Outputs:

- `D:\CodexBuilds\thallbyssal-lab\reports\current-best-runtime-evidence-matrix.json`
- `D:\CodexBuilds\thallbyssal-lab\reports\current-best-runtime-evidence-matrix.md`

The tool reads historical `ThallbyssalProductRuntimeProbe` reports and accepts both UTF-8 and UTF-16LE JSON files, because some older PowerShell-written reports use UTF-16LE encoding.

## Final Runtime Marker

The known-good playable beta reports this marker:

```text
CURRENT BEST A2 FULL-RIG / GATE V0.7 / OUT+0.3 / PG8 / TW-OFF / CF2 / GS1 / PSET2 / BST1 / CHF1
```

The active build also previously reported `CHUG-UI2` in the longer UI/runtime marker. Source recovery should treat `PSET2 / BST1 / CHF1 / CHUG-UI2` as the final known-good direction, not older `PSET1` checkpoints.

## Measurement Evidence

The generated runtime evidence matrix found all expected historical runtime reports.

Stable 48 kHz generated-input window, block sizes >= 256:

| Milestone | Marker family | Clips | 48k stable peak mean | 48k stable RMS mean | Peak delta from final | RMS delta from final |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| `auto-level-v03` | A2 full-rig / Gate v0.6 | 0 | `0.899144` | `0.457347` | `-0.033623` | `-0.018458` |
| `peak-guard-v02` | A2 full-rig / Gate v0.6 | 0 | `0.899144` | `0.457347` | `-0.033623` | `-0.018458` |
| `ui-runtime-latest` | A2 full-rig / OUT+0.3 / PG8 / TW-OFF / CF2 | 0 | `0.928203` | `0.472785` | `-0.004565` | `-0.003020` |
| `pset2-bst1-chf1` | A2 full-rig / Gate v0.7 / OUT+0.3 / PG8 / TW-OFF / CF2 / GS1 / PSET2 / BST1 / CHF1 | 0 | `0.932767` | `0.475805` | `0.000000` | `0.000000` |

## Interpretation

The known-good beta is not merely "raw A2 NAM plus gain." It has a stable output behavior around `0.93` linear peak with zero clipping and a slightly higher RMS than the earlier v0.6 checkpoints.

The diagnostic source probe already proved that:

- old Live V1 gain staging is about `29-30 dB` too quiet;
- the high A2 gain region can match loudness direction but clips heavily without product safety;
- simple `peak-normalize`, `hard-ceiling`, and `soft-ceiling` remove clipping but do not recover the known-good beta balance.

Therefore, the missing source behavior is likely the exact A2 full-rig output polish/headroom and `PSET2 / BST1 / CHF1 / CHUG-UI2` settings, not a standalone limiter.

## Recovery Target

The next source-recovery probe variant should target:

- marker family: `CURRENT BEST A2 FULL-RIG / GATE V0.7 / OUT+0.3 / PG8 / TW-OFF / CF2 / GS1 / PSET2 / BST1 / CHF1 / CHUG-UI2`;
- stable generated-input peak: roughly `0.93` linear;
- stable generated-input RMS: roughly `0.476` linear;
- clipped samples: `0`;
- latency: `0` samples;
- 48 kHz direct NAM path at 48 kHz host rate;
- 48 kHz NAM adapter path at 44.1 kHz and 96 kHz host rates.

## What This Does Not Prove

- It does not prove source parity.
- It does not prove tone parity.
- It does not approve installing a rebuilt beta over the known-good playable beta.
- It does not make private NAM/IR assets shippable.
- It does not replace founder listening.

## Next Safe Step

Implement the next diagnostic-only source recovery probe variant against this evidence matrix.

Do not integrate it into `PluginProcessor`, do not install it over the known-good beta, and do not use it for owner listening until source-parity renders and runtime marker checks pass.
